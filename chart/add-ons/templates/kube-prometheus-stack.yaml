{{- if .Values.jenkins.enabled }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: jenkins
  namespace: {{ .Values.global.argocd_namespace | default "argocd"}}
  annotations:
    {{- if .Values.kubePrometheusStack.syncwave }}
    argocd.argoproj.io/sync-wave: {{ .Values.kubePrometheusStack.syncwave | quote}}
    {{- end }}
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: {{ .Values.global.repoURL}}
    targetRevision: HEAD
    path: add-ons/kube-prometheus-stack
    helm: {}
  destination:
    name: ''
    namespace: {{ .Values.kubePrometheusStack.namespace | default "monitoring" }}
    server: 'https://kubernetes.default.svc'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  {{- end }}